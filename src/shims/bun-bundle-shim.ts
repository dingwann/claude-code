// Shim that replaces bun:bundle's feature() for development
// This gets preloaded via bunfig.toml
// It patches the global module registry so `import { feature } from './bun-bundle-shim'`
// returns our runtime function instead of bun's compile-time-only one

const ENABLED_FLAGS = new Set([
  'BG_SESSIONS', 'BRIDGE_MODE', 'VOICE_MODE', 'BUDDY',
  'FORK_SUBAGENT', 'TRANSCRIPT_CLASSIFIER', 'BASH_CLASSIFIER',
  'TREE_SITTER_BASH_SHADOW', 'TEAMMEM', 'MCP_SKILLS',
  'EXPERIMENTAL_SKILL_SEARCH', 'CACHED_MICROCOMPACT',
  'WEB_BROWSER_TOOL', 'MONITOR_TOOL', 'AUTO_THEME',
  'HISTORY_SNIP', 'WORKFLOW_SCRIPTS', 'CONNECTOR_TEXT',
  'TOKEN_BUDGET', 'AGENT_MEMORY_SNAPSHOT', 'VERIFICATION_AGENT',
  'UPLOAD_USER_SETTINGS', 'CCR_REMOTE_SETUP', 'CCR_MIRROR',
  'CCR_AUTO_CONNECT', 'MEMORY_SHAPE_TELEMETRY', 'COMPUTER_USE',
  'MCP_TOOLS', 'PLUGINS', 'SKILLS', 'AUTO_MODE', 'PLAN_MODE',
  'TEAMS', 'WORKTREES', 'DAEMON_BG', 'CHUNKED_MCP',
  'CODE_EDITOR_TOOLS', 'CODE_EDITOR_PERMISSIONS', 'SCHEDULED_TASKS',
  'BRIEF_MODE', 'PROGRESS_SUMMARIES', 'BUDGET_CONTINUATION',
  'ABLATION_BASELINE', 'DUMP_SYSTEM_PROMPT', 'CHICAGO_MCP',
  'DAEMON', 'TEMPLATES', 'BYOC_ENVIRONMENT_RUNNER',
  'SELF_HOSTED_RUNNER', 'COORDINATOR_MODE', 'KAIROS',
  'MEMDIR', 'MEMORY_SHAPE_TELEMETRY', 'TOKEN_BUDGET',
])

export function feature(flag: string): boolean {
  return ENABLED_FLAGS.has(flag)
}
